def verify_opp_move():
    while True:
        if but2_pressed[0]:
            but2_pressed[0] = False  # Reset the flag
            # CHECK IF THE MOVE WAS CORRECT
            
            break

        time.sleep(0.1)